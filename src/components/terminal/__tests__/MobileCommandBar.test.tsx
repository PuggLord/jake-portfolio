// portfolio/src/components/terminal/__tests__/MobileCommandBar.test.tsx
import { describe, it } from 'vitest'

describe('MobileCommandBar', () => {
  // MOBL-01
  it.todo('renders a button for each MOBILE_COMMAND: help, about, projects, blog, contact, resume, clear (MOBL-01)')
  it.todo('each chip button has aria-label matching "Run {cmd} command" (MOBL-01)')
  it.todo('clicking a chip calls onCommand with the command name (MOBL-01)')
})
