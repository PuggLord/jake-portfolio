import { describe, it } from 'vitest'

describe('App — theme initialization (Phase 5, EXTR-02)', () => {
  it.todo('when localStorage "jake-os-theme" is "amber", data-theme attribute is set to "amber" before first render')
  it.todo('when localStorage "jake-os-theme" is missing, data-theme attribute is set to "green"')
  it.todo('when localStorage "jake-os-theme" is an invalid value, data-theme attribute falls back to "green"')
})
